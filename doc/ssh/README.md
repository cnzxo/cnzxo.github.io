# 如何使用cmd命令登录linux服务器？

## 1. 打开命令提示符

> 按快捷键 win+r打开运行，然后输入cmd打开命令行

## 2. ssh命令的使用

> 输入以下命令进行登录
~~~
ssh 用户名@主机名
~~~
> 例如：
~~~
ssh test@deat.cn
~~~
> 出现如下提示后输入密码（不显示），然后按回车键登录。 
~~~
test@deat.cn's password:
~~~

> 出现如下界面即登录成功，若登录失败请检查用户名或密码是否填写正确
